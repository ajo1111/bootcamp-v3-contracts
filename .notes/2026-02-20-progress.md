# Daily Progress Notes - February 20, 2026

> Deployment troubleshooting notes for `bootcamp-v3-contracts`.

## What We Worked On Today

1. Verified Hardhat configuration variables and missing values with:
   - `npx hardhat vars setup`
2. Set the required collector key:
   - `npx hardhat vars set COLLECTOR_PRIVATE_KEY`
3. Fixed Tenderly network account configuration in `hardhat.config.js`:
   - switched `accounts` from object shape to array shape
   - normalized private keys by trimming whitespace and ensuring `0x` prefix
4. Resolved Token deployment blockers on Tenderly:
   - `HH8` invalid `networks.tenderly.accounts` config
   - `IGN410` insufficient funds by funding the collector account in Tenderly dashboard
5. Resolved Exchange deployment blocker:
   - `IGN723` (`getAccount(1)` out of range when only one unique signer is available)
   - updated module to use parameterized fee account fallback:
     - `const FEE_ACCOUNT = m.getParameter("feeAccount", DEPLOYER);`

---

## Files Updated

1. `hardhat.config.js`
2. `ignition/modules/Exchange.js`

---

## Deployment Commands Used

1. `npx hardhat vars setup`
2. `npx hardhat vars set COLLECTOR_PRIVATE_KEY`
3. `npx hardhat ignition deploy ignition/modules/Token.js --network tenderly`
4. `npx hardhat ignition deploy ignition/modules/Exchange.js --network tenderly`

---

## Notes / Follow-up

1. If deploying with multiple role-based wallets, confirm both keys resolve to different addresses before relying on `m.getAccount(1)`.
2. Ensure the deployer account has native token balance on the target Tenderly network for gas.
3. Any exposed private key should be rotated immediately and reset via `hardhat vars`.

---

## References

1. https://tenderly.co/
2. https://hardhat.org/hardhat-runner/docs/guides/configuration-variables
3. https://hardhat.org/tutorial/deploying-to-a-live-network
4. https://dappuniversity.notion.site/Tenderly-Deployment-18ce460af681806bb32fd9224ed949bf
